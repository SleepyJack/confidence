{
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@supabase/supabase-js": "^2"
  },
  "devDependencies": {
    "dotenv": "^17.3.1",
    "jest": "^29"
  },
  "scripts": {
    "test": "jest --testPathIgnorePatterns=integration",
    "test:integration": "DOTENV_CONFIG_PATH=.env.local SUPABASE_SCHEMA=test node -r dotenv/config node_modules/.bin/jest --testPathPattern=integration --runInBand",
    "test:all": "npm test && npm run test:integration"
  }
}
